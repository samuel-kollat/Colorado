@model Colorado.Models.FilterHasNbarProtocol

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>FilterHasNbarProtocol</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Filter.name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Filter.name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.NbarProtocol.protocol_name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.NbarProtocol.protocol_name)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index", new { id = Model.filter_id })
        </div>
    }
</div>
